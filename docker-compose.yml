services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3080:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - external-network

networks:
  external-network:
    external: true
    name: zero-trust